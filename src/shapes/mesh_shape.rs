use std::path::Path;

use crate::{Shape, VertexBuffer};

pub struct MeshShape {
    vertex: VertexBuffer,
}

impl MeshShape {
    // pub fn from_path(path: Path) -> Self {
    // }
}

// impl Shape for MeshShape {
//     fn get_vertex_buffer(&self) -> &VertexBuffer { &self.vertex }
// }
